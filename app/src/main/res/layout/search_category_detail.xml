<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/category_detail_coordinator"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <LinearLayout
        android:id="@+id/category_stores_container"
        android:background="@color/secondaryLightColor"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:id="@+id/iv_category_detail_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:layout_margin="@dimen/margin_medium"
            android:tint="@drawable/selector_image_button_black"
            android:src="@drawable/ic_keyboard_arrow_left_24px" />

        <include layout="@layout/search_category_detail_header" />
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>